# Telemetry

HakusCLI (HakusAgent fork) ships telemetry **disabled by default**. The
upstream collection endpoint does not exist in this fork; nothing is sent
unless explicitly enabled in config. This file exists as the anchor for the
bundled telemetry test fixtures.
