[package]
name = "hakus-telemetry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Anonymous, user-disableable product usage counting for Hakus"

[dependencies]
anyhow.workspace = true
chrono.workspace = true
hakus-config = { path = "../config", version = "0.9.10" }
hakus-paths = { path = "../paths", version = "0.9.10" }
hakus-release = { path = "../release", version = "0.9.10" }
fd-lock = "4.0.4"
reqwest = { workspace = true, features = ["blocking"] }
serde.workspace = true
serde_json.workspace = true
tempfile.workspace = true
tracing.workspace = true
uuid.workspace = true

[build-dependencies]
hakus-build-support = { path = "../build-support", version = "0.9.10" }

[dev-dependencies]
# Used as an *assertion*, never as a filter: every string leaf of a serialized
# batch is run through `redact_for_disclosure` and must come back unredacted.
hakus-workflow = { path = "../workflow", version = "0.9.10" }
