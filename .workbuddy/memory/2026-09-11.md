# 2026-09-11

## 桌面端 UI 大改造（HakusAgent/frontend/desktop-tauri + frontend/terminal/crates/tui）

用户需求（经逐条提问确认）：修复添加自定义模型商报错、表单减负、加测试连接/拉取模型按钮、发送按钮统一、去星星图标、灰色列表改液态玻璃。

完成内容：
1. **Base URL 报错修复**：`runtime_api.rs::validate_provider_base_url` 与 `config_persistence.rs::normalize_custom_provider_base_url` 均自动补 `https://`；前端表单输入即时校验提示（红色描边+大白话）。
2. **表单减负**：ModelPanel 添加自定义模型商表单删除"唯一 ID"输入（`src/lib/provider-id.ts` 自动生成：名称 slug → 域名 slug → 'provider'，避开内置 id）；必填只留名称+接口地址；分组/接口格式/环境变量收进默认折叠"高级选项"。FirstRunSetup 同步改造（删 customId、自动 ID、URL 规范化）。
3. **测试连接/获取模型列表**：后端新增免创建探测端点 `POST /v1/providers/probe` 与 `/probe/models`（`probe_scoped_config` 用 Custom 身份 + override，不落盘）；前端 `apiClient.probeCustomEndpoint/probeCustomEndpointModels`；添加表单内联测试结果 + 拉取模型勾选列表（勾选结果作为 models 传入创建）。
4. **不再默认多出模型**：`provider_models_for_api` 对 `ApiProvider::Custom` 不再注入全局 default_model（根因：persist_custom_provider 会把新商设为激活商，触发 default_model 注入）。
5. **发送按钮统一**：新建 `src/components/ui/send-button.tsx`（圆形上箭头，禁用=暗色低对比，主题色实心可用）；替换 Composer 主发送、WeChatPanel 测试发送、ChatView launchpad 装饰按钮（ArrowUp）。
6. **去星星**：EmptyStateHero 星星删除；Sparkles→Bot（launchpad mark、MessageBubble AI 头像、FirstRunSetup mark/step）；WandSparkles→Puzzle（skills、composer mention）；About→Info；Composer tip 小星删除。
7. **液态玻璃列表**：index.css 新增 `.glass-card`/`.glass-card-body`/`.glass-chip`（磨砂 blur+主题色描边+大圆角，深浅色自适应，支持 prefers-reduced-transparency）；批量替换 settings 下 10 个文件的灰色卡片（`rounded-xl border border-border bg-card/40` 等 pattern），McpPanel 展开区/图标底座，ModelPanel 概览行与高级选项盒。

验证：tsc --noEmit ✓、vite build ✓、cargo check ✓（config_persistence 测试另跑）。

注意：axum 静态段 `probe` 与 `{id}` 参数路由共存优先级为静态优先。

## 同日晚间：应用户要求撤销当日全部更改（方案 A，外科手术式）

用户晚间要求"撤销你之前的所有更改"。仓库最后一次提交为 9/6（393c27e），工作区混有更早会话的未提交改动，故未用 git checkout，而是逐文件精确逆向今日改动：
- 逆向了 runtime_api.rs（validate_provider_base_url、probe 端点/路由、Custom 默认模型注入排除）、config_persistence.rs（URL 规范化）、client.ts（probe 方法）、ModelPanel（表单恢复原状：ID 手填、无测试按钮、原 JSX）、FirstRunSetup、Composer/ChatView/MessageBubble/SettingsDialog/SkillsPanel/AboutPanel/WeChatPanel（图标与发送按钮恢复）、index.css（删除 .glass-card 块）、10 个设置面板的 glass-card 批量替换（用 difflib 对 HEAD 行 forward()==working 逐行匹配逆向）、删除新建文件 send-button.tsx / provider-id.ts。
- 验证：tsc 通过；cargo check 另跑；残留扫描仅剩 ModelPanel.tsx:651 的 `model-provider-overview-row glass-card ...` 一处。
- **重要发现**：该处 glass-card 及 `!enabled && 'opacity-65'` 结构并非本次会话所写（会话中该行原为 `rounded-2xl border px-4 py-3 transition-colors` + enabled 条件），是会话结束后（9/11 9:47–22:18 之间）被外部（用户或另一会话）修改的。因 .glass-card CSS 已删，该 class 现无样式效果，需用户决定去留。
- 经验教训：接手"撤销"类任务前必须先核实工作区中哪些改动属于本次会话——mtime 会被覆盖不可靠，应以会话中精确的 edit old/new 字符串为逆向依据；对疑似会话后外部改动要停下来询问，不可凭 git 一刀切。
